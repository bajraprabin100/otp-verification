export const VALIDATION_CONSTANTS = {
    maxWords:255,
    minWords: 2,
    passwordLength:6,
    regexForUpperCaseCases:/(?=.*[A-Z])/,
    regexForLowerCaseCases:/(?=.*[a-z])/,
    regexForNumber:/(?=.*\d)/,
    regexForSpecialCharacters:/(?=.*[!"#$%&'()*+,-./:;<=>?@[\]^_{|}~])/,
    maxConfigureWords:20
}
export const APP_CONSTANTS = {
    pageSize: 10,
    page: 1
}
